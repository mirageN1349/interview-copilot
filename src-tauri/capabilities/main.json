{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Permissions for the main product window",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:allow-open",
    "sql:allow-load",
    "global-shortcut:allow-is-registered",
    "global-shortcut:allow-register",
    "global-shortcut:allow-unregister"
  ]
}
